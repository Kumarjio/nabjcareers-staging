{subject}{$GLOBALS.user_site_url}: Subscription For User {$user.username} Has Just Expired{/subject}
{message}
	<p>Subscription to {$plan.name} plan for User {$user.username} has just expired.</p>
	<p>{$GLOBALS.settings.site_title}</p>
{/message}